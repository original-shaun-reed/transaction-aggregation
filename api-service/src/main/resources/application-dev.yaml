# ── dev profile ────────────────────────────────────────────────
spring:
  config:
    activate:
      on-profile: dev


springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true
logging:
  level:
    za.co.reed.apiservice: DEBUG
    org.springframework.security: DEBUG
